import {readState,writeState} from './persistence';
const KEY='radar-sources';const fallback={sources:[]};
const allowedTypes=['public_feed','public_search','aggregate_trends','opt_in_leads'];
export async function listRadarSources(){return (await readState(KEY,fallback)).sources}
export async function saveRadarSource(input={}){if(!allowedTypes.includes(input.type))throw new Error('source_type_not_allowed');if(!input.name)throw new Error('source_name_required');const state=await readState(KEY,fallback);const now=new Date().toISOString();const existing=input.id?state.sources.find(x=>x.id===input.id):null;const source={...(existing||{}),id:input.id||crypto.randomUUID(),name:String(input.name),type:input.type,url:input.url||null,status:input.status||existing?.status||'inactive',privacyMode:input.type==='opt_in_leads'?'consent_required':'public_or_aggregate_only',createdAt:existing?.createdAt||now,updatedAt:now,lastCollectedAt:existing?.lastCollectedAt||null,lastError:input.lastError===undefined?(existing?.lastError||null):input.lastError};const i=state.sources.findIndex(x=>x.id===source.id);if(i>=0)state.sources[i]=source;else state.sources.unshift(source);await writeState(KEY,state);return source}
export async function updateRadarSource(id,changes={}){const current=(await listRadarSources()).find(x=>x.id===id);if(!current)return null;return saveRadarSource({...current,...changes,id})}
export async function markSourceCollection(id,{ok,error}={}){const current=(await listRadarSources()).find(x=>x.id===id);if(!current)return null;const at=new Date().toISOString();return saveRadarSource({...current,id,lastCollectedAt:at,lastError:ok?null:String(error||'collection_failed'),updatedAt:at})}
export const radarSourceTypes=allowedTypes;
