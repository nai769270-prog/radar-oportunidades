function canonical(text=''){return String(text).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/https?:\/\/\S+/g,' ').replace(/[^a-z0-9 ]/g,' ').replace(/\s+/g,' ').trim()}
function tokens(signal={}){const stop=new Set(['para','com','uma','que','por','dos','das','the','and','www','http','https']);return [...new Set(canonical(`${signal.title||''} ${signal.text||''}`).split(' ').filter(x=>x.length>2&&!stop.has(x)))].slice(0,40)}
export function fingerprint(signal={}){return canonical(`${signal.title||''} ${signal.text||''}`).slice(0,220)}
function similarity(a,b){const A=new Set(tokens(a)),B=new Set(tokens(b));if(!A.size||!B.size)return 0;let common=0;for(const x of A)if(B.has(x))common++;return common/Math.min(A.size,B.size)}
function cleanUrl(v=''){try{const u=new URL(v);u.hash='';['utm_source','utm_medium','utm_campaign','utm_term','utm_content','fbclid','gclid'].forEach(k=>u.searchParams.delete(k));return `${u.hostname}${u.pathname}${u.search}`.replace(/\/$/,'')}catch{return String(v||'').split('#')[0]}}
export function dedupeSignals(signals=[]){const kept=[],urls=new Set();for(const s of signals){const url=cleanUrl(s.sourceUrl||'');if(url&&urls.has(url))continue;if(kept.some(k=>similarity(k,s)>=.78))continue;kept.push(s);if(url)urls.add(url)}return kept}
