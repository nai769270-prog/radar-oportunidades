const supported=['whatsapp-ai','ai-chatbot','instagram-service'];
export function channelFor(project={}){const id=project.solution?.id;if(id==='whatsapp-ai')return 'whatsapp';if(id==='instagram-service')return 'instagram';if(id==='ai-chatbot')return 'webchat';return null}
export function initialChannelLifecycle(project={}){const channel=channelFor(project);if(!supported.includes(project.solution?.id)||!channel)return null;return {channel,status:'not_connected',connected:false,tested:false,approved:false,active:false,updatedAt:new Date().toISOString(),checks:{officialAccountAuthorized:false,credentialsConfigured:false,webhookConfigured:false,handoffValidated:false,testConversationPassed:false}}}
export function nextChannelLifecycle(current,action){const c={...current,checks:{...(current?.checks||{})},updatedAt:new Date().toISOString()};if(action==='connect'){c.connected=true;c.status='connected';c.checks.officialAccountAuthorized=true;c.checks.credentialsConfigured=true;c.checks.webhookConfigured=true;return c}if(action==='test'){if(!c.connected)throw new Error('channel_not_connected');c.tested=true;c.status='tested';c.checks.handoffValidated=true;c.checks.testConversationPassed=true;return c}if(action==='approve'){if(!c.tested)throw new Error('channel_not_tested');c.approved=true;c.status='approved';return c}if(action==='activate'){if(!c.approved)throw new Error('channel_not_approved');c.active=true;c.status='active';c.activatedAt=new Date().toISOString();return c}if(action==='deactivate'){c.active=false;c.status=c.approved?'approved':'connected';return c}throw new Error('invalid_channel_action')}
