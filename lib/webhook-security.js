import crypto from 'crypto';
function safeEqual(a,b){const aa=Buffer.from(String(a||''));const bb=Buffer.from(String(b||''));return aa.length===bb.length&&crypto.timingSafeEqual(aa,bb)}
export function verifySharedSecret(request,provider){const key=`WEBHOOK_SECRET_${String(provider||'').toUpperCase().replace(/[^A-Z0-9]/g,'_')}`;const expected=process.env[key];if(!expected)return {ok:false,error:'webhook_secret_not_configured',env:key};const supplied=request.headers.get('x-radar-webhook-secret');if(!supplied)return {ok:false,error:'webhook_signature_missing'};return safeEqual(supplied,expected)?{ok:true}:{ok:false,error:'webhook_signature_invalid'}}
export function webhookSetup(provider){const key=`WEBHOOK_SECRET_${String(provider||'').toUpperCase().replace(/[^A-Z0-9]/g,'_')}`;return {provider,requiredEnv:[key],note:'Este adaptador fica bloqueado até a validação oficial do provedor ser implementada/configurada. Não use segredo compartilhado genérico como substituto da assinatura oficial em produção.'}}
