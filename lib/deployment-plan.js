function slug(value='cliente'){return String(value).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,48)||'cliente'}
export function createDeploymentPlan(project,workspace){const builds=(workspace?.artifacts||[]).filter(a=>Array.isArray(a.files)&&a.files.length);const build=builds.at(-1);if(!build)return {ready:false,reason:'build_required'};if(project.solution?.id!=='website')return {ready:false,reason:'website_only_for_now'};return {ready:true,provider:'vercel',mode:'preview',projectName:`radar-${slug(project.requirements?.businessName||project.client?.name)}`,files:build.files.map(f=>({file:f.path,data:f.content})),checks:['Revisar textos, preços e horários','Confirmar canal de contato do cliente','Validar autorização para publicação','Publicar primeiro como preview'],requiresApproval:true,sourceArtifactId:build.id}}
