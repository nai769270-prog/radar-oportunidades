import {NextResponse} from 'next/server';
const weights={urgent:100,high:70,medium:40,low:10};
const impact={goal:30,checkout:28,commercial:26,validation:24,opportunity:22,production:20,delivery_setup:18,delivery:12};
function norm(v=''){return String(v).trim().toLowerCase()}
export async function GET(request){try{const base=new URL(request.url);const[actionRes,performanceRes]=await Promise.all([fetch(new URL('/api/action-center',base.origin),{cache:'no-store'}),fetch(new URL('/api/category-performance',base.origin),{cache:'no-store'})]);if(!actionRes.ok)throw new Error('action_center_unavailable');const payload=await actionRes.json();const performance=performanceRes.ok?(await performanceRes.json()).data||[]:[];const performanceMap=new Map(performance.map(x=>[norm(x.category),x]));const actions=payload.data?.actions||[];const ranked=actions.map(a=>{const category=a.category||a.meta?.category||null;const learned=category?performanceMap.get(norm(category)):null;const learnedBoost=learned?Math.min(20,Math.round((Number(learned.efficiencyScore||0)*Number(learned.confidence||0)/100)*.2*10)/10):0;return {...a,category,learnedEfficiency:learned?{score:learned.efficiencyScore,confidence:learned.confidence,revenuePerAttempt:learned.revenuePerAttempt}:null,learnedBoost,dailyScore:Math.round(((weights[a.priority]||0)+(impact[a.type]||0)+learnedBoost)*10)/10}}).sort((a,b)=>b.dailyScore-a.dailyScore);const selected=[];const types=new Set();for(const item of ranked){if(selected.length>=3)break;if(types.has(item.type)&&ranked.some(x=>!types.has(x.type)&&!selected.some(s=>s.id===x.id)))continue;selected.push(item);types.add(item.type)}for(const item of ranked){if(selected.length>=3)break;if(!selected.some(x=>x.id===item.id))selected.push(item)}return NextResponse.json({data:{date:new Date().toISOString().slice(0,10),tasks:selected.map((x,i)=>({...x,order:i+1})),remaining:Math.max(0,actions.length-selected.length),summary:payload.data?.summary||{},goals:payload.data?.goals||null},meta:{rule:'Até 3 tarefas por dia. Prioridade combina urgência, impacto comercial e, quando há categoria e histórico suficiente, eficiência observada. O bônus aprendido é limitado a 20 pontos e reduzido pela confiança.',automaticExecution:false}})}catch(e){return NextResponse.json({error:e.message},{status:500})}}
