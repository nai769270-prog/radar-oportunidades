import {NextResponse} from 'next/server';
import {getProject,saveProject} from '../../../lib/store';
export async function POST(request){const {projectId,url,deploymentId,projectId:externalProjectId}=await request.json();const project=getProject(projectId);if(!project)return NextResponse.json({error:'project_not_found'},{status:404});if(project.deployment?.status!=='approved')return NextResponse.json({error:'preview_not_approved'},{status:400});if(!url)return NextResponse.json({error:'deployment_url_required'},{status:400});const deployment={...(project.deployment||{}),status:'deployed',url:String(url),deploymentId:deploymentId||null,externalProjectId:externalProjectId||null,deployedAt:new Date().toISOString()};saveProject({...project,deployment});return NextResponse.json({data:deployment})}
