import {NextResponse} from 'next/server';
import {listDigitalProducts,saveDigitalProduct,updateDigitalProduct,deactivateDigitalProduct} from '../../../lib/product-catalog';
export async function GET(){return NextResponse.json({data:listDigitalProducts()})}
export async function POST(request){const body=await request.json();if(!body?.title)return NextResponse.json({error:'product_title_required'},{status:400});try{const product=saveDigitalProduct(body);return NextResponse.json({data:product},{status:201})}catch(e){return NextResponse.json({error:e.message},{status:400})}}
export async function PATCH(request){const body=await request.json();if(!body?.id)return NextResponse.json({error:'product_id_required'},{status:400});try{const product=body.action==='deactivate'?deactivateDigitalProduct(body.id):updateDigitalProduct(body.id,body.changes||{});if(!product)return NextResponse.json({error:'product_not_found'},{status:404});return NextResponse.json({data:product})}catch(e){return NextResponse.json({error:e.message},{status:400})}}
