'use client';
import {useEffect,useMemo,useState} from 'react';
const initialOpportunities=[{id:'demo-1',title:'Preciso automatizar atendimento no WhatsApp',source:{name:'Sinal público'},intent:'Alta',score:92,status:'new'},{id:'demo-2',title:'Procuro sistema para organizar leads',source:{name:'Comunidade pública'},intent:'Alta',score:87,status:'new'}];
const score=o=>Number(o.opportunityScore??o.score??0);const oid=o=>String(o.id||o.categoryId||'');
export default function Home(){const[query,setQuery]=useState('');const[items,setItems]=useState(initialOpportunities);const[saved,setSaved]=useState({});const[matches,setMatches]=useState({});const[loading,setLoading]=useState(false);const[view,setView]=useState('new');async function loadSaved(){try{const r=await fetch('/api/saved',{cache:'no-store'}),p=await r.json();if(r.ok)setSaved(Object.fromEntries((p.data||[]).map(x=>[String(x.id),x])))}catch{}}async function matchHistory(list){if(!list.length){setMatches({});return}try{const compact=list.slice(0,50).map(o=>({id:o.id||o.categoryId,title:o.title||o.category,category:o.category,sourceUrl:o.sourceUrl||o.source?.url,problem:o.problem,text:o.text,evidence:o.evidence}));const r=await fetch(`/api/saved?match=1&items=${encodeURIComponent(JSON.stringify(compact))}`,{cache:'no-store'}),p=await r.json();if(r.ok)setMatches(Object.fromEntries((p.data||[]).map(x=>[String(x.id),x])))}catch{setMatches({})}}useEffect(()=>{loadSaved();matchHistory(initialOpportunities)},[]);const merged=useMemo(()=>items.map(o=>{const id=oid(o),m=matches[id],previous=(m?.matchedId&&saved[String(m.matchedId)])||saved[id];return previous||m?.matched?{...o,status:previous?.status||m?.status||'new',savedAt:previous?.savedAt,lastSeenAt:previous?.lastSeenAt,seenBefore:true,matchedId:m?.matchedId||previous?.id,matchType:m?.matchType||'id'}:{...o,seenBefore:false}}),[items,saved,matches]);const groups=useMemo(()=>({new:merged.filter(o=>!o.seenBefore),active:merged.filter(o=>o.seenBefore&&['new','analyzing'].includes(o.status)),approved:merged.filter(o=>o.status==='approved'),discarded:merged.filter(o=>o.status==='discarded')}),[merged]);const visible=groups[view]||[];async function search(e){e?.preventDefault();if(!query.trim())return;setLoading(true);try{let r=await fetch('/api/live-discovery',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({topic:query})}),p=await r.json();if(!p.meta?.live){r=await fetch(`/api/opportunities?q=${encodeURIComponent(query)}`);p=await r.json()}const next=p.data||[];setItems(next);await loadSaved();await matchHistory(next);setView('new')}finally{setLoading(false)}}async function saveItem(o){const id=oid(o);if(!id)return null;const r=await fetch('/api/saved',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...o,id,title:o.title||o.category||'Oportunidade'})}),p=await r.json();if(r.ok){setSaved(x=>({...x,[String(p.data.id)]:p.data}));setMatches(x=>({...x,[id]:{id,matched:true,matchedId:p.data.id,status:p.data.status,matchType:p.matchedBy||'id'}}))}return p.data}async function status(o,next){let target=o.matchedId||oid(o);if(!o.seenBefore){const created=await saveItem(o);target=created?.id||target}const r=await fetch('/api/saved',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:target,status:next})}),p=await r.json();if(r.ok){setSaved(x=>({...x,[String(target)]:p.data}));setMatches(x=>({...x,[oid(o)]:{...(x[oid(o)]||{}),id:oid(o),matched:true,matchedId:target,status:next}}))}}async function analyze(o){await status(o,'analyzing');sessionStorage.setItem('radar:selected',JSON.stringify({...o,status:'analyzing'}));window.location.href='/opportunity'}return <main className="shell"><aside className="sidebar"><div className="brand">RADAR<span>AI</span></div><p>Inteligência de oportunidades</p><nav><b>◉ Visão geral</b><a href="/products/today">★ Plano do Dia</a><a href="/products/queue">▥ Pipeline</a><a href="/products/learning">✦ Aprendizado</a><a href="/products/dashboard">◫ Comercial</a></nav></aside><section className="content"><header><div><small>RADAR DE OPORTUNIDADES</small><h1>Novos sinais separados do que você já trabalhou.</h1><p>O Radar reconhece histórico por ID, URL e similaridade do conteúdo.</p></div></header><form className="search" onSubmit={search}><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Ex.: empresas procurando automação de WhatsApp"/><button disabled={loading}>{loading?'Buscando...':'Pesquisar sinais'}</button></form><div className="metrics"><article><span>Novas</span><strong>{groups.new.length}</strong></article><article><span>Em análise</span><strong>{groups.active.length}</strong></article><article><span>Aprovadas</span><strong>{groups.approved.length}</strong></article><article><span>Descartadas</span><strong>{groups.discarded.length}</strong></article></div><div className="panel"><div className="requirementsForm"><button className={view==='new'?'open':''} onClick={()=>setView('new')}>🆕 Novas ({groups.new.length})</button><button className={view==='active'?'open':''} onClick={()=>setView('active')}>🔎 Já vistas / análise ({groups.active.length})</button><button className={view==='approved'?'open':''} onClick={()=>setView('approved')}>✓ Aprovadas ({groups.approved.length})</button><button className={view==='discarded'?'open':''} onClick={()=>setView('discarded')}>✕ Descartadas ({groups.discarded.length})</button></div>{visible.length===0?<div className="empty">Nenhuma oportunidade nesta seção.</div>:visible.sort((a,b)=>score(b)-score(a)).map((o,i)=>{const id=oid(o)||String(i);return <div className="row" key={id}><div className="score">{score(o)}</div><div className="problem"><strong>{o.title||o.category||'Oportunidade'}</strong><span>{o.source?.name||o.source||o.category||'Radar'} · {o.seenBefore?`Já reconhecido${o.matchType&&o.matchType!=='id'?` por ${o.matchType}`:''}`:'Novo sinal'}</span><small>Status: <strong>{o.status==='analyzing'?'ANALISANDO':o.status==='approved'?'APROVADO':o.status==='discarded'?'DESCARTADO':'NOVO'}</strong></small>{(o.sourceUrl||o.source?.url)&&<a href={o.sourceUrl||o.source.url} target="_blank" rel="noreferrer">Ver evidência ↗</a>}</div><span className={'tag '+(score(o)>=85?'hot':'')}>{score(o)>=85?'HOT':score(o)>=70?'FORTE':'OBSERVAR'}</span><button className="open" onClick={()=>analyze(o)}>Analisar →</button>{!o.seenBefore&&<button onClick={()=>saveItem(o)}>Salvar</button>}{o.status!=='approved'&&<button onClick={()=>status(o,'approved')}>Aprovar</button>}{o.status!=='discarded'&&<button onClick={()=>status(o,'discarded')}>Descartar</button>}{o.status==='discarded'&&<button onClick={()=>status(o,'new')}>Restaurar</button>}</div>})}</div></section></main>}
