'use client';
import {useEffect,useMemo,useState} from 'react';
export default function SalesDashboard(){const[plan,setPlan]=useState(null);const[events,setEvents]=useState([]);const[deliveries,setDeliveries]=useState([]);const[loading,setLoading]=useState(true);async function refresh(){setLoading(true);try{const r=await fetch('/api/sales-status',{cache:'no-store'});const p=await r.json();if(r.ok){setEvents(p.data?.events||[]);setDeliveries(p.data?.deliveries||[])}}finally{setLoading(false)}}useEffect(()=>{try{setPlan(JSON.parse(sessionStorage.getItem('radar:digital-sales-plan')||'null'))}catch{}refresh()},[]);const metrics=useMemo(()=>{const paid=events.filter(x=>x.status==='paid'),refunds=events.filter(x=>x.status==='refunded');return{sales:paid.length,refunds:refunds.length,revenue:paid.reduce((s,x)=>s+Number(x.amount||0),0)-refunds.reduce((s,x)=>s+Number(x.amount||0),0),deliveries:deliveries.filter(x=>x.status==='delivered').length,ready:deliveries.filter(x=>x.status==='ready').length}},[events,deliveries]);if(!plan)return <main className="detail"><a href="/products">← Produtos Digitais</a><div className="panel"><h1>Configure vendas primeiro.</h1></div></main>;return <main className="detail"><a href="/products/sales">← Configuração de vendas</a><section className="panel"><small>PAINEL DE VENDAS</small><h1>{plan.product?.title}</h1><p>Provedor: <strong>{plan.provider}</strong> · Status: <strong>{plan.status}</strong></p><button className="open" onClick={refresh} disabled={loading}>{loading?'Atualizando...':'Atualizar dados'}</button></section><section className="metrics"><article><span>Vendas aprovadas</span><strong>{metrics.sales}</strong><small>pagamentos verificados no servidor</small></article><article><span>Receita registrada</span><strong>R$ {metrics.revenue.toFixed(2)}</strong><small>menos reembolsos registrados</small></article><article><span>Entregas acessadas</span><strong>{metrics.deliveries}</strong><small>{metrics.ready} aguardando acesso</small></article><article><span>Reembolsos</span><strong>{metrics.refunds}</strong><small>eventos registrados</small></article></section><section className="panel"><small>ENTREGAS</small><h2>Acesso aos materiais</h2>{deliveries.length?deliveries.map(d=><div className="row" key={d.id}><div className="problem"><strong>{d.status==='delivered'?'Material acessado':d.status==='expired'?'Link expirado':'Aguardando acesso'}</strong><span>{d.buyerEmail||'E-mail indisponível'} · {d.accessCount||0} acesso(s)</span>{d.lastAccessAt&&<span>Último acesso: {new Date(d.lastAccessAt).toLocaleString('pt-BR')}</span>}</div><span className={'status '+(d.status==='delivered'?'ready':'')}>{d.status}</span></div>):<div className="empty">Nenhuma entrega registrada.</div>}</section><section className="panel"><small>EVENTOS</small><h2>Últimas movimentações</h2>{events.length?events.map(e=><div className="row" key={e.id}><div className="problem"><strong>{e.status==='paid'?'Pagamento aprovado':e.status==='refunded'?'Reembolso':'Pagamento pendente'}</strong><span>{e.provider} · {e.receivedAt?new Date(e.receivedAt).toLocaleString('pt-BR'):'—'}</span></div><b>R$ {Number(e.amount||0).toFixed(2)}</b></div>):<div className="empty">Nenhum pagamento verificado recebido ainda.</div>}</section></main>}
