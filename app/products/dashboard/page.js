'use client';
import {useEffect,useMemo,useState} from 'react';
export default function SalesDashboard(){const[plan,setPlan]=useState(null);const[events,setEvents]=useState([]);useEffect(()=>{try{setPlan(JSON.parse(sessionStorage.getItem('radar:digital-sales-plan')||'null'));setEvents(JSON.parse(sessionStorage.getItem('radar:sales-events')||'[]'))}catch{}},[]);const metrics=useMemo(()=>{const paid=events.filter(x=>x.status==='paid'),refunds=events.filter(x=>x.status==='refunded');return{sales:paid.length,refunds:refunds.length,revenue:paid.reduce((s,x)=>s+Number(x.amount||0),0)-refunds.reduce((s,x)=>s+Number(x.amount||0),0),deliveries:events.filter(x=>x.deliveryStatus==='delivered').length}},[events]);if(!plan)return <main className="detail"><a href="/products">← Produtos Digitais</a><div className="panel"><h1>Configure vendas primeiro.</h1></div></main>;return <main className="detail"><a href="/products/sales">← Configuração de vendas</a><section className="panel"><small>PAINEL DE VENDAS</small><h1>{plan.product?.title}</h1><p>Provedor: <strong>{plan.provider}</strong> · Status: <strong>{plan.status}</strong></p></section><section className="metrics"><article><span>Vendas aprovadas</span><strong>{metrics.sales}</strong><small>pagamentos confirmados</small></article><article><span>Receita líquida registrada</span><strong>R$ {metrics.revenue.toFixed(2)}</strong><small>menos reembolsos registrados</small></article><article><span>Entregas</span><strong>{metrics.deliveries}</strong><small>materiais liberados</small></article><article><span>Reembolsos</span><strong>{metrics.refunds}</strong><small>eventos registrados</small></article></section><section className="panel"><small>EVENTOS</small><h2>Últimas movimentações</h2>{events.length?events.map(e=><div className="row" key={e.id}><div className="problem"><strong>{e.status==='paid'?'Pagamento aprovado':e.status==='refunded'?'Reembolso':'Pagamento pendente'}</strong><span>{e.provider} · {e.receivedAt?new Date(e.receivedAt).toLocaleString('pt-BR'):'—'}</span></div><b>R$ {Number(e.amount||0).toFixed(2)}</b></div>):<div className="empty">Nenhum evento real recebido ainda. O painel ficará preenchido quando o webhook do provedor estiver integrado.</div>}</section></main>}
