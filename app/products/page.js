'use client';
import {useEffect,useState} from 'react';
const samples=[{id:'d1',source:'tendência agregada',query:'receitas saudáveis e marmita saudável',signalType:'aggregate'},{id:'d2',source:'post público',text:'como estudar melhor para concurso e montar cronograma de estudos',signalType:'public'},{id:'d3',source:'tendência agregada',query:'planilha gastos organizar orçamento',signalType:'aggregate'}];
export default function Products(){const[items,setItems]=useState([]);const[ranking,setRanking]=useState([]);const[categories,setCategories]=useState([]);const[loading,setLoading]=useState(false);const[query,setQuery]=useState('');useEffect(()=>{fetch('/api/digital-demand').then(r=>r.json()).then(p=>setCategories(p.data?.categories||[]));analyze(samples)},[]);async function analyze(input){setLoading(true);try{const [a,b]=await Promise.all([fetch('/api/digital-demand',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items:input})}),fetch('/api/demand-ranking',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items:input})})]);const p=await a.json(),r=await b.json();setItems(p.data||[]);setRanking(r.data||[])}finally{setLoading(false)}}function run(e){e.preventDefault();if(!query.trim())return;analyze([{id:crypto.randomUUID(),source:'entrada para análise',query,signalType:'public_or_aggregate'}])}function createProduct(x){const signal=items.find(i=>i.categoryId===x.categoryId)||x;sessionStorage.setItem('radar:digital-product',JSON.stringify(signal));window.location.href='/products/create'}return <main className="shell"><aside className="sidebar"><div className="brand">RADAR<span>AI</span></div><p>Inteligência de oportunidades</p><nav><a href="/">◉ Visão geral</a><b>◆ Produtos Digitais</b><a href="/products/catalog">▤ Catálogo</a><a href="/products/dashboard">◫ Vendas</a><a href="/projects">▦ Projetos</a></nav></aside><section className="content"><header><div><small>RADAR DE PRODUTOS DIGITAIS</small><h1>Transforme demanda pública em <em>produtos úteis.</em></h1><p>Analise tendências agregadas e sinais públicos, encontre temas com demanda e prepare produtos digitais sem usar histórico privado de pesquisa.</p></div><a className="linkBtn" href="/products/catalog">Gerenciar catálogo</a></header><form className="search" onSubmit={run}><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Ex.: receitas saudáveis, cronograma de estudos, organizar gastos"/><button disabled={loading}>{loading?'Analisando...':'Analisar demanda'}</button></form><div className="panel"><small>RANKING DE OPORTUNIDADE</small><h2>O que vale investigar primeiro</h2>{ranking.length?ranking.map((x,i)=><div className="row" key={x.categoryId}><div className="score">{x.opportunityScore}</div><div className="problem"><strong>#{i+1} · {x.category}</strong><span>{x.signalCount} sinal(is) · {x.sourceCount} fonte(s) · confiança {x.confidence}</span><p>Produto sugerido: <b>{x.productSuggestion}</b></p></div><button className="open" onClick={()=>createProduct(x)}>Criar produto</button></div>):<div className="empty">Ainda não há sinais suficientes para formar ranking.</div>}</div><div className="panel"><small>CATEGORIAS</small><h2>Mercados monitoráveis</h2><div className="connectorGrid">{categories.map(c=><div className="connector" key={c.id}><strong>{c.name}</strong><span className="status ready">{c.risk==='health'?'revisão especial':'ativo'}</span><small>{c.product}</small></div>)}</div></div><div className="panel"><small>SINAIS ANALISADOS</small><h2>Evidências classificadas</h2>{!items.length?<div className="empty">Nenhuma correspondência encontrada.</div>:items.map(x=><div className="row" key={x.id}><div className="score">{x.score}</div><div className="problem"><strong>{x.category}</strong><span>{x.source} · {x.signalType}</span><p>Produto: <b>{x.productSuggestion}</b></p>{x.risk==='health'&&<small>Conteúdo educacional geral; sem inferir condição individual ou prometer resultado de saúde.</small>}</div></div>)}</div><div className="panel"><small>POLÍTICA DO RADAR</small><h2>De onde a demanda pode vir</h2><p>Permitido: tendências agregadas, publicações públicas, leads com opt-in e contatos comerciais publicados. Não usamos histórico privado de pesquisa nem tentamos descobrir a identidade de quem fez uma busca privada.</p></div></section></main>}
