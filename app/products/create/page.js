'use client';
import {useEffect,useMemo,useState} from 'react';
function slug(v='produto'){return v.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')}
export default function CreateProduct(){const[signal,setSignal]=useState(null);const[title,setTitle]=useState('');const[price,setPrice]=useState('27,90');const[generated,setGenerated]=useState(null);useEffect(()=>{try{const x=JSON.parse(sessionStorage.getItem('radar:digital-product')||'null');setSignal(x);setTitle(x?.product?.title||x?.productSuggestion||'')}catch{}},[]);const sections=useMemo(()=>signal?.product?.sections||['Introdução','Passo a passo','Exemplos','Checklist','Próximos passos'],[signal]);function generate(){const health=signal?.risk==='health';setGenerated({id:crypto.randomUUID(),slug:slug(title),title,price,category:signal?.category||'Produto digital',chapters:sections.map((name,i)=>({name,summary:i===0?'Apresentação do objetivo, para quem é o material e como utilizá-lo.':`Conteúdo prático de ${name.toLowerCase()}, com exemplos e ações aplicáveis.`})),offer:{headline:`${title}: um guia prático para começar`,subheadline:health?'Material educacional geral para ajudar na organização da rotina, sem promessas de resultado individual.':'Um material direto, organizado e feito para transformar informação em ação.',cta:`Quero acessar por R$ ${price}`},reviewRequired:true,healthDisclaimer:health?'Este material é educacional e não substitui orientação individual de profissional qualificado.':null});}function makeLanding(){if(!generated)return;sessionStorage.setItem('radar:digital-product-build',JSON.stringify({...signal,generated}));window.location.href='/products/landing'}if(!signal)return <main className="detail"><a href="/products">← Produtos Digitais</a><div className="panel"><h1>Selecione uma demanda primeiro.</h1></div></main>;return <main className="detail"><a href="/products">← Produtos Digitais</a><section className="panel"><small>FÁBRICA DE PRODUTOS</small><h1>{signal.category}</h1><p>Demanda analisada: <strong>{signal.source}</strong> · score {signal.score}</p></section><section className="panel"><small>CONFIGURAÇÃO</small><h2>Preparar produto</h2><div className="requirementsForm"><label>Nome do produto<input value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Preço inicial (R$)<input value={price} onChange={e=>setPrice(e.target.value)}/></label><div className="full"><strong>Estrutura sugerida</strong>{sections.map((x,i)=><div className="workTask" key={x}><b>{i+1}</b><div><strong>{x}</strong></div></div>)}</div><div className="formActions"><button onClick={generate}>Gerar estrutura do produto</button></div></div></section>{generated&&<><section className="panel"><small>PRODUTO GERADO</small><h2>{generated.title}</h2><p>Preço inicial: <strong>R$ {generated.price}</strong></p>{generated.healthDisclaimer&&<p className="apolloError">{generated.healthDisclaimer}</p>}{generated.chapters.map((c,i)=><article className="artifactCard" key={c.name}><small>CAPÍTULO {i+1}</small><h3>{c.name}</h3><p>{c.summary}</p></article>)}</section><section className="panel"><small>OFERTA</small><h2>{generated.offer.headline}</h2><p>{generated.offer.subheadline}</p><button onClick={makeLanding}>Criar página de venda →</button></section></>}</main>}
