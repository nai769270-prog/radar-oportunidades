'use client';
import {useEffect,useState} from 'react';
const providers=[['hotmart','Hotmart'],['kiwify','Kiwify'],['stripe','Stripe'],['mercadopago','Mercado Pago']];
const checkLabels={contentReviewed:'Conteúdo revisado',claimsReviewed:'Promessas e afirmações revisadas',privacyPublished:'Política de privacidade pronta',refundPolicyPublished:'Política de reembolso pronta',checkoutTested:'Checkout testado',deliveryTested:'Entrega testada'};const emptyChecks=()=>Object.fromEntries(Object.keys(checkLabels).map(k=>[k,false]));
export default function Sales(){const[data,setData]=useState(null);const[provider,setProvider]=useState('hotmart');const[checkoutUrl,setCheckoutUrl]=useState('');const[assetUrl,setAssetUrl]=useState('');const[checks,setChecks]=useState(emptyChecks());const[message,setMessage]=useState('');const[saving,setSaving]=useState(false);useEffect(()=>{let x=null;try{x=JSON.parse(sessionStorage.getItem('radar:digital-product-build')||'null');setData(x)}catch{}if(x?.generated?.id)fetch('/api/sales-config?productId='+encodeURIComponent(x.generated.id),{cache:'no-store'}).then(r=>r.json()).then(p=>{const c=p.data;if(!c)return;setProvider(c.provider||'hotmart');setCheckoutUrl(c.checkoutUrl||'');setAssetUrl(c.assetUrl||'');setChecks({...emptyChecks(),...(c.checks||{})});setMessage('Configuração comercial recuperada do armazenamento.')}).catch(()=>{})},[]);if(!data?.generated)return <main className="detail"><a href="/products">← Produtos Digitais</a><div className="panel"><h1>Crie um produto primeiro.</h1></div></main>;const ready=Boolean(checkoutUrl&&assetUrl&&Object.values(checks).every(Boolean));async function save(status=ready?'ready_for_activation':'draft'){setSaving(true);setMessage('');try{const payload={productId:data.generated.id,provider,checkoutUrl,assetUrl,checks,status,queueItemId:data.queueItemId||null};const r=await fetch('/api/sales-config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const p=await r.json();if(!r.ok)throw new Error(p.error||'Falha ao salvar configuração');sessionStorage.setItem('radar:digital-sales-plan',JSON.stringify({...payload,id:p.data.id,savedAt:p.data.updatedAt}));setMessage('Configuração comercial salva com segurança.');return p.data}catch(e){setMessage(e.message);return null}finally{setSaving(false)}}async function prepare(){if(!ready)return;const saved=await save('ready_for_activation');if(!saved)return;try{if(data.queueItemId){const r=await fetch('/api/opportunity-queue',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:data.queueItemId,status:'completed'})});if(!r.ok)throw new Error('Não foi possível concluir a oportunidade no pipeline.')}setMessage('Configuração comercial validada e persistida. Oportunidade concluída no Pipeline. A ativação real ainda depende da integração/autorização do provedor de checkout.')}catch(e){setMessage(e.message)}}return <main className="detail"><a href="/products/landing">← Página de venda</a><section className="panel"><small>VENDA E ENTREGA</small><h1>{data.generated.title}</h1><p>Configure checkout e entrega antes de ativar vendas.</p>{message&&<p className="successMsg">{message}</p>}</section><section className="panel"><small>CHECKOUT</small><h2>Plataforma de pagamento</h2><div className="requirementsForm"><label>Provedor<select value={provider} onChange={e=>setProvider(e.target.value)}>{providers.map(([id,name])=><option key={id} value={id}>{name}</option>)}</select></label><label>URL oficial do checkout<input value={checkoutUrl} onChange={e=>setCheckoutUrl(e.target.value)} placeholder="Cole o checkout criado na plataforma"/></label><label className="full">Arquivo/link de entrega<input value={assetUrl} onChange={e=>setAssetUrl(e.target.value)} placeholder="Link seguro do ebook ou área de membros"/></label></div><p>O Radar não coleta número de cartão. O pagamento deve ocorrer no checkout do provedor escolhido.</p></section><section className="panel"><small>VALIDAÇÃO</small><h2>Checklist para ativação</h2>{Object.entries(checkLabels).map(([k,label])=><label className={'workTask '+(checks[k]?'done':'')} key={k}><input type="checkbox" checked={checks[k]} onChange={e=>setChecks({...checks,[k]:e.target.checked})}/><div><strong>{label}</strong><small>{checks[k]?'Concluído':'Pendente'}</small></div></label>)}<div className="formActions"><button className="open" disabled={saving} onClick={()=>save()}>{saving?'Salvando...':'Salvar configuração'}</button><button disabled={!ready||saving} onClick={prepare}>Validar e concluir →</button></div><p className={ready?'successMsg':'apolloError'}>{ready?'Tudo validado para a próxima etapa de integração.':'Ainda existem configurações ou testes pendentes.'}</p></section></main>}
