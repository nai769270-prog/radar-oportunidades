'use client';
import {useEffect,useState} from 'react';
export default function Project(){const[data,setData]=useState(null);const[done,setDone]=useState(new Set());useEffect(()=>{const id=sessionStorage.getItem('radar:project');if(!id)return;fetch('/api/project-workspace',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId:id})}).then(r=>r.json()).then(p=>setData(p.data))},[]);function toggle(id){setDone(prev=>{const n=new Set(prev);n.has(id)?n.delete(id):n.add(id);return n})}if(!data)return <main className="detail"><a href="/projects">← Projetos</a><div className="panel"><h1>Carregando projeto...</h1></div></main>;const{project,workspace}=data;return <main className="detail"><a href="/projects">← Projetos</a><div className="panel"><small>{project.solution?.name}</small><h1>{project.client?.name}</h1><p>Workspace de produção e entrega.</p></div><div className="detailGrid"><section className="panel"><h2>Checklist de construção</h2>{workspace.tasks.map(t=><div className={'workTask '+(done.has(t.id)?'done':'')} key={t.id}><input type="checkbox" checked={done.has(t.id)} onChange={()=>toggle(t.id)}/><div><strong>{t.title}</strong><small>{done.has(t.id)?'Concluída':t.status==='active'?'Em andamento':'Pendente'}</small></div></div>)}</section><aside className="panel"><h2>Entrega</h2><p>Etapa: <strong>{project.status}</strong></p><h3>Entregáveis</h3>{project.deliverables?.map(x=><span className="deliverable" key={x}>{x}</span>)}<h3>Artefatos</h3><p>{workspace.artifacts.length?'Arquivos disponíveis.':'Ainda não há arquivos gerados.'}</p></aside></div></main>}
